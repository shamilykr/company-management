import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#311b92",
      dark: "#002884",
      contrastText: "#fff",
    },
    secondary: {
      main: "#f44336",
      dark: "#ba000d",
      contrastText: "#000",
    },
  },
});
